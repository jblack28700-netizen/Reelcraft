// FrameSource is an abstract seam; it has no implementation beyond the
// declarations in FrameSource.h. This translation unit exists to register the
// source file in the build (Phase 3 Objective 3).
#include "FrameSource.h"
